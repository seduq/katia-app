/**
 * Filters -> Date -> Employee
 * Table -> Table Row -> Schedules -> New schedule
 */

import * as React from "react";

export interface ISchedulesPageProps {}

export default function SchedulesPage(props: ISchedulesPageProps) {
  return <main></main>;
}
